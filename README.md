# This final project is about Dockerize ETL Pipeline using ETL tools Airflow that extract Public API data, then load into MySQL (Staging Area) and finally aggregate the data and save into PostgreSQL.
